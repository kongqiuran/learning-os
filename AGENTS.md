# AGENTS.md

## 协作规则

1. **每次完成文件改动后都要尝试提交并推送到 GitHub。** 默认执行 `git status`、`git add`、`git commit`、`git push`，不需要先询问用户。完成后必须明确告知用户：
   - commit 是否成功；
   - git push 是否成功；
   - 如果 push 失败，失败原因是什么，并要求用户手动处理；
   - 同时提供用户可以继续执行的命令。

2. **每次回复都要说明本次改动内容。** 尤其要清楚列出：
   - 改了哪些文件；
   - 改动位置或模块；
   - 实现了什么功能或修复了什么问题；
   - 是否影响前端、后端、数据库、部署配置或文档。

3. **不要隐瞒失败。** 如果命令执行失败、测试失败、推送失败、部署失败，必须直接说明失败点、错误信息和下一步需要谁处理。不要把未完成的操作描述成已完成。

4. **能自动完成的事情优先自动完成。** 包括读取文件、修改代码、运行检查、提交、推送等。只有遇到账号登录、权限授权、云服务商控制台、GitHub 凭证、支付平台、第三方后台等必须用户操作的事情，才让用户手动处理。

5. **修改前先理解现有代码。** 不要在未查看相关文件的情况下大改。优先沿用项目已有结构、命名、风格和业务约定，避免引入不必要的新框架或复杂方案。

6. **保持改动尽量小而清晰。** 只改和任务直接相关的内容；如果发现额外问题，可以提醒用户，但不要擅自扩大范围。

7. **语言规则：**
   - 面向用户的说明用中文；
   - `.md` 文档以中文为主；
   - `.py` / `.js` / `.ts` 等代码中的变量名、函数名、注释和错误类型优先使用英文；
   - 如果是产品界面文案，遵循现有项目的中英文/i18n 结构。

8. **命令说明规则：** 如果需要用户复制执行命令，代码块第一行必须先进入项目目录：

   ```bash
   cd "D:\kongqiuran\learning-os\learning-os"
   ```

9. **测试与验证：** 代码改动后应根据影响范围尽量运行合适的检查，例如单元测试、语法检查、构建或启动检查。如果没有运行测试，要说明原因。

10. **安全与隐私：** 不要提交真实密钥、Token、密码、Cookie、支付凭证或用户隐私数据。涉及 `.env`、密钥、服务器凭证时只给出占位符和配置说明。

11. **部署规则：** 如果本地 `git push` 成功，并且本次改动影响线上运行功能，应继续按项目现有部署流程更新服务器；如果只是文档或注释改动，可以说明无需部署。部署失败时停止后续步骤并告知用户手动处理。

12. **最终汇报格式建议：**
   - 改动文件；
   - 实现/修改内容；
   - 检查或测试结果；
   - Git commit / push 结果；
   - 是否需要用户继续操作。
